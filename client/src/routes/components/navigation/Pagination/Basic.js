import React from "react";
import {Card, Pagination} from "antd";

const Basic = () => {
  return (
      <Pagination  showTitle={true}  defaultCurrent={1} total={50}/>
 
  );
};

export default Basic;
